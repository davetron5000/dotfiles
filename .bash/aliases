alias vi='vim'
alias hk='herkou'

function ffexa() {
  if [ -z $1 ]; then
    exa -F
  else
    if [ $1 == "-ltr" ]; then
      \ls $*
    else
      echo $1
      exa $*
    fi
  fi
}
alias ls=ffexa

# vim: ft=sh
